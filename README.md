# Ai-Task-Manager
A production-ready AI Task Manager that processes natural language tasks through a LangGraph multi-agent pipeline, generates structured outputs using local LLMs (Ollama), and enables semantic search with FAISS vector storage. Includes Streamlit UI, CLI, and Docker support.
